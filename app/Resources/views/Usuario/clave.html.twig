{% extends 'layout.html.twig' %}

{% block title %}

{% endblock %}

{% block content %}
    <h1>Modificar la clave</h1>

    {{ form_start(formulario) }}
    {{ form_widget(formulario) }}
    <div>
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <button type="submit" class="btn btn-success"><i class="fa fa-check"></i>Guardar</button>
            <a href="{{ path('inicio') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i>Volver</a>
        </div>
    </div>
    {{ form_end(formulario) }}
{% endblock %}
