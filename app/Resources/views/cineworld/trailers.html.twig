{% extends 'layout.html.twig' %}

{% block title 'trailer' %}

{% block content %}

    <h1>Lista de Trailers</h1>

    <p>
        <a href="{{ path('trailermas') }}" class="btn btn-success">Añadir Trailer</a>
    </p>
    <table border="1px" width=>
        <thead>
        <tr>
            <th>Titulo</th>
        </tr>
        </thead>
        <tbody>
        {% for trailer in trailerss %}
            <tr>
                <td><a href="{{ path('trailer', {'id': trailer.id}) }}">{{ trailer.nombre}}</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}